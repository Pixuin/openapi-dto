
This update ensures that the `CodeGenerator` is instantiated with the correct namespace, and the test checks for the namespace in the generated class. 

After making this change, you should be able to run the tests again using:

```bash
composer test
```